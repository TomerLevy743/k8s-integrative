apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tomer-app-ingress
  namespace: tomer
spec:
  ingressClassName: nginx
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wordpress-service
                port:
                  number: 80
